"""Stablecoin Compliance Agent (sca).

Three layers, nothing free-floating:
  tools   — deterministic facts (no LLM)
  corpus  — curated, human-gated reasoning frame
  agent   — synthesis of facts + frame, every claim cited
"""

__version__ = "0.1.0"
